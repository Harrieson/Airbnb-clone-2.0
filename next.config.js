/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
}

module.exports = nextConfig

module.exports = {
    experimental: {
        urlImports: ["https://links.papareact.com/"]
    }
}

module.exports = {
    images: {
        domains: [
            "links.papareact.com",
        ]
    }
}